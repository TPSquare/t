/** @type {import('next').NextConfig} */

const nextConfig = { 
  devIndicators: false, 
  experimental: {
    allowedDevOrigins: ["https://karamuse.onrender.com"]
  }
};


export default nextConfig;
